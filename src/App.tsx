import styled from "styled-components";
import Timer from "./components/Timer";

const App = () => {
  return (
    <AppContainer>
      <Timer />
    </AppContainer>
  );
};

export default App;

const AppContainer = styled.div``;
